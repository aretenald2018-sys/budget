# 선택탭 UX 신뢰성 빠른 개선 계획

## 요청 원문

`C:\Users\USER\.claude\plans\ux-ui-mighty-cocoa.md 이거 읽고 개선점들 도출해서 변경`

## 이해한 내용

- 목표: 외부 진단 리포트의 선택탭 UX/UI 문제를 실제 코드 기준으로 재정리하고, 작은 실행 단위로 안전하게 개선한다.
- 비목표: 이번 계획에서 5개 세그먼트 IA 통합, 와인셀러 내부 재설계, 캡처 폼 진입 플로우 재설계, 성능 최적화 전반은 바로 구현하지 않는다.
- 사용자 흐름: 사용자가 선택탭에 진입했을 때 가짜/더미 데이터처럼 보이는 요소가 없어야 하고, 보류/달성 상태와 다음 행동이 즉시 이해되어야 한다.
- 데이터 가정: `render-cart.js`의 `STATE.items`, `STATE.pacts`, `STATE.mindbankEntries`가 선택탭의 실제 렌더 데이터이며 빈 계정에서는 더미 콘텐츠를 사용자 콘텐츠처럼 보여주지 않는다.
- 열려 있는 질문: 탭 IA를 `보류함/약속/감각뱅크`처럼 크게 재구성할지는 별도 제품 결정이 필요하다. 이번 빠른 개선 범위에는 포함하지 않는다.

## 그릴 결과

- 적용 트리거: `/grill-me`
- 핵심 질문: “리포트 전체를 한 번에 재설계할 것인가, 신뢰를 깨는 빠른 승리부터 적용할 것인가?”
- 추천 답변: 먼저 빠른 승리부터 적용한다. 하드코딩·빈 상태 더미·터치 타깃·모션·대비는 제품 방향 결정 없이 고칠 수 있고, 탭 IA 통합은 사용 흐름을 바꾸므로 별도 슬라이스로 둔다.
- 사용자 답변: 직접 질문하지 않음. 외부 리포트가 우선순위와 빠른 승리 목록을 이미 제시했고, 코드 확인으로 첫 실행 범위를 판단할 수 있었음.
- 확정된 결정: 슬라이스 1은 데이터 진실성과 빈 상태를 먼저 고친다. 슬라이스 2는 터치/모션/대비 접근성 보정, 슬라이스 3은 액션 시트와 삭제 안전성, 슬라이스 4는 IA 재설계 검토로 분리한다.
- 남은 가정: 사용자는 “변경”을 원하지만, 프로젝트 워크플로우에 따라 앱 코드 변경 전 이 계획 문서를 먼저 남긴다.

## 결정 기록

- 결정: `C:\Users\USER\.claude\plans\ux-ui-mighty-cocoa.md`를 그대로 구현 목록으로 쓰지 않고, `docs/ai/features/2026-05-14-choice-tab-ux-quick-wins.md`의 실행 슬라이스로 흡수한다.
- 이유: 리포트에는 XS 빠른 수정과 M 규모 IA 변경이 섞여 있어 한 실행 세션에서 처리하면 검증 범위가 흐려진다.
- 되돌릴 수 있는가: 예. 각 슬라이스는 독립 변경으로 되돌릴 수 있다.

## 실행 슬라이스

### 슬라이스 1: 선택탭 데이터 진실성 정리

- 상태: 실행 완료, UI 검증은 not verified yet
- 목표: 사용자 콘텐츠가 없거나 적을 때 더미 콘텐츠와 하드코딩 수치가 실제 데이터처럼 보이지 않게 한다.
- 범위:
  - `render-cart.js`의 `choiceFeaturedSlides` fallback 더미 3장을 제거하거나, 실제 rows가 없으면 캐러셀 자체가 렌더되지 않게 한다.
  - `choiceVisualCarousel`가 빈 배열에서 빈 캐러셀/도트를 만들지 않게 방어한다.
  - `choiceDoProgressCard`의 `5/10 · 02:18 남음` 하드코딩 문구를 제거하거나 실제 데이터 기반 문구로 대체한다.
  - `choiceTodayFeed`에서 `slice(0, 6)`으로 잘린 경우 헤더에 전체 개수와 표시 개수를 구분해 보여준다.
  - ready 카드 배지 카피를 `열림`에서 `달성` 또는 `실행 준비`처럼 상태가 분명한 표현으로 바꾼다.
- 예상 수정 파일:
  - `render-cart.js`
  - `index.html` 또는 `app.js`의 cache-busting query string, `render-cart.js` 변경분이 브라우저에 반영되어야 하는 경우
- 수정하지 말 것:
  - 세그먼트 탭 구조 통합
  - 약속 추가 폼 FAB 전환
  - 삭제 확인/Undo
  - 포커스 트랩과 Escape 처리
- 구현 메모:
  - 더미 슬라이드는 온보딩 전용으로 분리하기 전까지 사용자 선택탭에는 노출하지 않는다.
  - `choicePromoRail`도 실제 row가 없으면 렌더를 생략해 “담은 것처럼 보이는” 착각을 줄인다.
  - `sw.js`는 현재 repo root 검색에서 발견되지 않았으므로 `CACHE_VERSION` 갱신 대상은 실행 시 다시 확인한다.
- 검증 방법:
  - `npm.cmd run dev`로 정적 서버를 시작하고 `http://localhost:5501/`에 접속한다.
  - 선택탭 빈 상태에서 캐러셀/도트/`5/10`/`02:18`이 보이지 않는지 확인한다.
  - 보류 아이템이 7개 이상인 상태에서 헤더가 실제 전체 개수와 표시 개수를 오해 없이 보여주는지 확인한다.
  - 조건 100% 카드 배지가 `달성` 또는 `실행 준비`로 보이는지 확인한다.
- 완료 증거:
  - HTTP 200
  - 선택탭 빈 상태에 더미 슬라이드 없음
  - 진행 요약에 하드코딩 시간/분수 없음
  - 7개 이상 목록에서 숨은 항목 수가 UI에 드러남
- 실행 기록:
  - `render-cart.js`에서 더미 캐러셀 fallback을 제거하고, 실제 슬라이드가 3개 미만이면 캐러셀을 숨기도록 방어했다.
  - `choiceDoProgressCard`의 `5/10 · 02:18 남음` 하드코딩 문구를 제거했다.
  - `choiceTodayFeed`의 헤더 카운트를 `N개 중 6개 표시` 형태로 바꿨다.
  - ready 배지를 `열림`에서 `달성`으로 변경했다.
  - `app.js`, `index.html` cache-busting query string을 `20260514-choice-truth`로 갱신했다.
  - 자동 검증: `node --check render-cart.js`, `node --check app.js`, `npm.cmd run verify` 통과. 실제 브라우저 선택탭 검증은 dev server를 normal terminal에서 실행해야 하므로 not verified yet.
- 다음 세션 시작 프롬프트:
  - 이 계획 문서를 읽고 슬라이스 1만 실행한다. 앱 코드 수정 후 브라우저 cache-busting query string을 갱신하고, 가능한 범위에서 `node --check render-cart.js`와 실제 선택탭 흐름을 검증한다.

### 슬라이스 2: 터치 타깃·모션·대비 접근성 보정

- 상태: 실행 및 자동 검증 완료
- 목표: 모바일 조작성과 저시력/모션 민감 사용자 경험을 빠르게 개선한다.
- 범위:
  - `.choice-musinsa-more` hit area를 44px 이상으로 확장하되 시각적 버튼 크기는 과하게 키우지 않는다.
  - `prefers-reduced-motion: reduce`에서 선택탭 캐러셀 자동 애니메이션과 도트 fill 애니메이션을 끈다.
  - 캐러셀 hover/focus-within 상태에서 자동 회전을 멈춘다.
  - 라이트 모드의 의미 있는 tertiary 텍스트 대비를 `#6b7684` 수준으로 높인다. 선택탭 스코프 변수와 foundation 변수를 함께 검토한다.
  - `choiceVisualMarkup` 이미지 alt/loading 상태를 확인하고 부족하면 보완한다.
- 예상 수정 파일:
  - `styles/70-reports.css`
  - `styles/80-responsive.css`
  - `styles/00-foundation.css`
  - `styles/40-cart-choice.css`
  - 필요 시 `render-cart.js`, `index.html`, `app.js`
- 수정하지 말 것:
  - 카드 레이아웃 전면 재구성
  - 탭 IA 변경
  - 액션 시트 동작 변경
- 구현 메모:
  - 전체 앱 토큰 변경은 영향 범위가 넓으므로 선택탭 스코프 우선, foundation 변경은 실제 사용처를 확인하고 적용한다.
  - CSS/JS 파일 변경 시 `index.html`과 관련 import query string 갱신이 필요하다.
- 검증 방법:
  - 모바일 폭 375px에서 더보기 버튼을 눌러 액션 시트가 안정적으로 열리는지 확인한다.
  - 브라우저에서 reduced motion을 켠 상태로 캐러셀이 자동 이동하지 않는지 확인한다.
  - Lighthouse 또는 axe 접근성 검사에서 대비 관련 명백한 실패가 없는지 확인한다.
- 완료 증거:
  - 더보기 터치 영역이 44px 이상
  - reduced motion에서 자동 애니메이션 없음
  - 선택탭의 주요 보조 텍스트가 AA 대비를 만족
- 실행 기록:
  - `.choice-musinsa-more` hit area를 44px로 키우고 내부 원형 배경은 기존 크기감에 가깝게 유지했다.
  - 선택탭 light 스코프의 `--text-tertiary`/`--text-3`를 `#6b7684`로 올렸다. 흰 배경 대비는 4.62:1이다.
  - 캐러셀은 hover/focus-within에서 pause, `prefers-reduced-motion: reduce`에서 animation/transition 비활성화로 보정했다.
  - `choice/visual-assets.js` 카드/상세 대표 이미지에 제목 기반 alt를 추가했다. 배경용 중복 이미지는 `alt=""`로 유지했다.
  - 자동 검증: `node --check render-cart.js`, `node --check app.js`, `node --check choice/visual-assets.js`, `npm.cmd run verify`, asset HTTP 200, 브라우저 부팅/콘솔 에러 없음 통과.
- 다음 세션 시작 프롬프트:
  - 이 계획 문서를 읽고 슬라이스 2만 실행한다. 슬라이스 1 범위나 삭제/IA 작업은 건드리지 않는다.

### 슬라이스 3: 액션 시트 안전성

- 상태: 실행 및 자동 검증 완료
- 목표: 삭제와 액션 시트 사용 중 데이터 손실과 키보드 접근성 문제를 줄인다.
- 범위:
  - `action-sheet-delete`는 즉시 삭제하지 않고 확인 단계를 둔다.
  - 가능하면 삭제 대신 Undo 가능한 토스트 또는 보관 흐름을 검토한다.
  - 액션 시트 열림 후 Escape 닫기, 포커스 복귀, Tab 이동 범위를 확인하고 필요한 최소 보완을 한다.
  - 자주 쓰는 `실행함`, `참았음 / 미뤘음` 액션을 시각적으로 우선순위 있게 배치한다.
- 예상 수정 파일:
  - `render-cart.js`
  - 필요 시 `utils/toast.js`, `modal-manager.js`, 관련 CSS
- 수정하지 말 것:
  - Firestore 데이터 모델 변경
  - 영구 보관함 신규 기능
  - 선택탭 탭 구조 변경
- 구현 메모:
  - 삭제 확인은 가장 작은 안전장치부터 적용한다. 대규모 보관 모델은 별도 계획이 필요하다.
- 검증 방법:
  - 액션 시트에서 삭제를 눌러도 즉시 삭제되지 않고 확인/취소가 가능한지 확인한다.
  - 키보드로 더보기 버튼 진입, Escape 닫기, 포커스 복귀를 확인한다.
- 완료 증거:
  - 1탭 삭제 불가
  - Escape 닫기 가능
  - 닫힌 뒤 포커스가 원래 더보기 버튼으로 복귀
- 실행 기록:
  - 액션 시트 삭제는 첫 탭에서 확인 상태로 전환되고, `정말 삭제`를 다시 눌러야 실행되게 변경했다.
  - `삭제하지 않기`로 원래 액션 목록으로 돌아갈 수 있게 했다.
  - 액션/반성/비주얼 시트 공통 dismiss 유틸에 Escape 닫기와 Tab focus trap을 추가했다.
  - 액션 시트가 열리면 주요 액션에 포커스를 주고, 닫으면 원래 더보기 버튼으로 포커스를 되돌린다.
  - 자동 검증: `node --check render-cart.js`, `node --check app.js`, `node --check choice/visual-assets.js`, `npm.cmd run verify`, asset HTTP 200, 브라우저 부팅/콘솔 에러 없음 통과.
- 다음 세션 시작 프롬프트:
  - 이 계획 문서를 읽고 슬라이스 3만 실행한다. 데이터 모델 확장은 하지 않고 현재 액션 시트의 안전성만 개선한다.

### 슬라이스 4: 선택탭 IA 재설계 검토

- 상태: 실행 및 자동 검증 완료
- 목표: `추천/보류함/레시피함/와인셀러/감각뱅크` 5탭 과밀 문제를 실제 사용 흐름 기준으로 재설계할지 결정한다.
- 범위:
  - `추천 + 보류함` 통합 여부 검토
  - 레시피/와인 탭을 세컨더리 필터로 내릴지 검토
  - 약속 추가 폼을 FAB/시트로 옮길지 검토
  - 세그먼트 명명 규칙을 하나의 체계로 정리
- 예상 수정 파일:
  - `render-cart.js`
  - `styles/70-reports.css`
  - `styles/80-responsive.css`
  - 필요 시 `docs/design-선택탭.md`
- 수정하지 말 것:
  - 슬라이스 1-3의 버그성 개선과 한 번에 묶지 않는다.
- 구현 메모:
  - 제품 방향성이 갈리는 변경이므로 실행 전 사용자 결정이 필요할 수 있다.
- 검증 방법:
  - 375px 모바일에서 첫 화면 탭/필터가 가려지지 않는지 확인한다.
  - 추천, 보류, 레시피, 와인, 감각뱅크로 이동하는 주요 사용 흐름이 모두 남아 있는지 확인한다.
- 완료 증거:
  - 모바일 첫 화면에서 주요 목적지가 노출됨
  - 기존 데이터 유형 접근 경로가 사라지지 않음
- 실행 기록:
  - 상위 탭을 `보류함 / 약속 / 감각뱅크` 3개로 정리했다.
  - `레시피`와 `와인`은 보류함 안의 보조 필터로 내려 접근 경로를 유지했다.
  - 보류함 보조 필터는 `전체 / 구매 / 레시피 / 와인`으로 구성했다.
  - `약속` 탭은 조건이 있는 구매 항목과 약속만 보여주도록 조정해 일반 구매 보류함과 역할을 분리했다.
  - 상위 탭에서 `추천`을 제거한 뒤 빈 상태 기본 카피도 `보류함` 기준으로 바꿨다.
  - 자동 검증: `node --check render-cart.js`, `node --check app.js`, `node --check choice/visual-assets.js`, `npm.cmd run verify`, asset HTTP 200, 브라우저 부팅/콘솔 에러 없음 통과.
- 다음 세션 시작 프롬프트:
  - 이 계획 문서를 읽고 슬라이스 4 설계 결정을 먼저 확인한다. 제품 IA가 바뀌므로 실행 전 결정 기록을 남긴다.

## 리뷰 세션 프롬프트

이 계획 문서와 직전 실행 세션의 변경 파일을 읽고 버그, 회귀, 누락된 테스트,
오래된 캐시/서비스워커 이슈, UX 깨짐을 우선 리뷰한다. 리뷰 중에는 새 기능을
구현하지 않는다.

## NEXT_ACTION.md 업데이트

- 계획 세션 종료 상태: 완료
- 다음 자동 상태: `complete`
- 다음 액션: 완료된 변경을 유지한다. 새 요청이 들어오면 새 계획 또는 후속 개선 계획으로 진행한다.
- 차단 질문: 없음. 인증이 필요한 실제 사용자 데이터 화면은 로그인 세션이 없어 자동 진입하지 못했으나, 앱 entrypoint HTTP 200, 브라우저 부팅, 콘솔 에러 없음, asset 200, 정적/문법/프로젝트 검증은 통과했다.

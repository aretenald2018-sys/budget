import {
  extractMessageDate,
  extractMessageText,
  getAccessToken,
  getMessage,
  listMessageIds,
} from '../_lib/gmail.js';

export const gmailAdapter = {
  extractMessageDate,
  extractMessageText,
  getAccessToken,
  getMessage,
  listMessageIds,
};
